// src/components/Header.js

import React from 'react';

const Header = () => {
    return (
        <header style={{ backgroundColor: '#25D366', padding: '10px', color: 'white' }}>
            <h1>WhatsApp Clone</h1>
        </header>
    );
};

export default Header;